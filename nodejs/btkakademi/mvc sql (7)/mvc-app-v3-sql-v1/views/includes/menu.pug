div.col-lg
        div.list-group
            if categories.length >0
                each category in categories
                    a(href=`/categories/${category.id}`, class=(path===`/categories/${category.id}` ? "list-group-item active" : "list-group-item")) #{category.name}
            else 
                h1 No Category