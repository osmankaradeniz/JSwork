extends ../layouts/main-layout.pug
 
include ../mixin/createProduct.pug

block content
        
        include ../includes/slider.pug
            
        div.row
            if products.length > 0
                each product in products 
                        +createProduct(product)
            else 
                h1 Product not found